// Main configuration file that includes all necessary configurations

// Include all default/common configurations
include "config/default/*.cfg"

// Include platform-specific configurations
include_platform {
  linux   = "config/linux/*.cfg"  // Linux-specific configurations
  darwin  = "config/darwin/*.cfg" # macOS-specific configurations
  windows = "config/windows/*.cfg" // Windows-specific configurations
}